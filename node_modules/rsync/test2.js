var Rsync = require('./rsync');


var rsync = new Rsync();
rsync.source(__dirname + '/test/file1.txt')
    .destination(__dirname + '/test/file2.txt')
    .progress();

    rsync.execute(function(error, stdout, stderr) {
        console.log('done');
        console.log(arguments);
    });
